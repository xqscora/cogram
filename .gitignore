
# Large / regenerable benchmark datasets (fetch separately, see README)
data/longmemeval_s_cleaned.json
data/
locomo10.json
benchmark_bakeoff_embeddings.json
__pycache__/
*.pyc

# Raw / derived copies of the public HF dataset — regenerate locally via
# fetch_swe.py + swe_to_transcripts.py instead of committing them.
# Source: https://huggingface.co/datasets/nebius/SWE-agent-trajectories (CC-BY-4.0)
swe_slice.jsonl
swe_corpus/

# Personal data — never commit (Cora's own chat history, not for public repo)
raw_store/
concept_graph.json
DEMO.md
demo_recommend.md
_demo_gap.txt
_demo_hit.txt

# Secrets — never commit
.env.local
.env
.env*.local
.engram_pending_gaps.json

# InsForge & AI agent skills
.insforge
.agent
.agents
.augment
.claude
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf

# pip install -e build artifact
cogram.egg-info/
